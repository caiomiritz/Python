#------------INPUT-------------------------

temos o método input, onde o parametro é a mensagem do console que aparece para o usuario
ele retorna oq escrevemos no console como resposta do input

o input sempre retorna uma string

podemos guardar a entrada do usuario em alguma variavel

ex: numero = input("Digite um número")

nesse exemplo, a variavel numero vai receber oq o usuario escreve

#---------------------CONDICIONAIS----------------

if, else if e else sao os condicionais, onde os parametros recebidos sao as condiçoes
o bloco que é executado apos passar nesse condicional, precisa vir após o ':' dps do condicional.

if(numero == 12):
    bloco de comando
else:
    bloco de comando

podemos notar que a comparação do numero é com uma string, porque o input retorna uma string
alem disso, temos o ':' que representa o bloco que vai ser executado. Todo o bloco precisa estar separado por 4 espaços
podemos ver que o "bloco de comando" esta separado

#---------------INT--------------------------

int é uma função que converte algo para um int
como tivemos o problema do input que sempre retorna uma string, precisamos fazer essa conversão

int() o parametro é o valor que queremos converter para int

